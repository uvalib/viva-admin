<%= form_with(model: viva, local: true) do |form| %>
  <% if viva.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(viva.errors.count, "error") %> prohibited this viva from being saved:</h2>

      <ul>
      <% viva.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :institution %>
    <%= form.text_field :institution %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
